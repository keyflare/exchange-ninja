package com.keyflare.exchange.core.navigation

import com.keyflare.exchange.mainScreen.api.mainScreenDestinations
import com.keyflare.exchange.navigation.core.api.NavigationDestinationsSet

val appDestinations = NavigationDestinationsSet(
    mainScreenDestinations
)
